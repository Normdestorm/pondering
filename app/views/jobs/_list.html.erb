<table class="table table-striped">
  <thead>
    <tr>
      <th>Job_Title</th>
      <th>Location</th>
      <th>Salary</th>
      <% if @user = current_user %>
        <th>Actions</th>
      <% end %>
    </tr>
  </thead>
  <tbody>
    <% @job_applications.each do |job_application|%>
      <tr>
        <td><%= job_application.job.job_title %></td>
        <td><%= job_application.job.location %></td>
        <td><%= job_application.job.salary %></td>
        <% if @user == current_user %>
        <%= job_application.class.name %>
          <td><%= link_to "Delete this job", job_application_path(job_application.id), 
                      method: :delete,
                      data: {confirm: "Are you sure?"}, 
                      class: "btn btn-xs btn-danger" %>
          </td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>